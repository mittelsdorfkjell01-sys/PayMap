# Legacy Code

Diese Ordner sind archivierter Code, der nicht mehr aktiv entwickelt wird.

- `api/`: Express-API (ersetzt durch Next.js API-Routes in `apps/nextjs/app/api/`)
- `web/`: Vite-React-App (ersetzt durch `apps/nextjs/`)

Behalten für: Referenz beim Nachbauen von UI-Snippets (z. B. `CityDetailsSheet.tsx`).
Nicht im Workspace, nicht im Build, keine Dependencies-Pflege.
